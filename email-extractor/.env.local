# No environment variables needed for the current setup
# We're using simple username/password authentication 

# Supabase configuration
# Sign up at https://supabase.com and create a new project
# Then copy the URL and anon key from the API settings page
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url_here
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Cron job configuration
# Set up a free account at https://cron-job.org
# Configure the job to call your /api/jobs/process-batch endpoint
# CRON_JOB_URL=your_app_url/api/jobs/process-batch 